server:
  port: ${PORT:8082}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus

rest-connector:
  routing-config-location: classpath:actions-routing.yml
