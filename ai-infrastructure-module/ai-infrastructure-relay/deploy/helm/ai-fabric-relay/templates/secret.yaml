apiVersion: v1
kind: Secret
metadata:
  name: {{ include "ai-fabric-relay.fullname" . }}-secrets
  labels:
    {{- include "ai-fabric-relay.labels" . | nindent 4 }}
type: Opaque
stringData:
  RELAY_API_KEY: {{ .Values.relay.auth.apiKey.value | quote }}
