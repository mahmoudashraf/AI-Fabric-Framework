apiVersion: v1
kind: Service
metadata:
  name: {{ include "ai-fabric-relay.fullname" . }}
  labels:
    {{- include "ai-fabric-relay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ai-fabric-relay.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
