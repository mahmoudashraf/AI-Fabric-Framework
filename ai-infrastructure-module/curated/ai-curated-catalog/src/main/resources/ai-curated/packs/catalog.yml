ai:
  orchestration:
    profile: PRODUCTION_NAVIGATOR
    modes:
      navigator:
        information-mode: DETERMINISTIC_RAG_GENERATE
        working-set-window-turns: 3
      navigator_deep:
        information-mode: DETERMINISTIC_RAG_GENERATE
        use-advanced-rag: true
        working-set-window-turns: 5
    position-routing:
      landing: navigator
      catalog: navigator
      search: navigator

  prompts:
    bundle:
      overlays:
        - v1-catalog
