ai:
  orchestration:
    profile: DEMO_CATALOG
    modes:
      navigator:
        information-mode: DETERMINISTIC_RAG_GENERATE
        prompt-mode: MINIMAL_FOR_RAG
    position-routing:
      landing: navigator
      cart: navigator
      support: navigator

  prompts:
    bundle:
      overlays:
        - v1-catalog
