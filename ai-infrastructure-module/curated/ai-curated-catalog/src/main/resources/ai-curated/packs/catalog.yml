ai:
  orchestration:
    profile: PRODUCTION_NAVIGATOR
    modes:
      navigator:
        information-mode: DETERMINISTIC_RAG_GENERATE
    position-routing:
      landing: navigator
      cart: navigator
      support: navigator

  prompts:
    bundle:
      overlays:
        - v1-catalog
